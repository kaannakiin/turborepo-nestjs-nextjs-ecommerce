"use client";

import { Group, Stack } from "@mantine/core";
import { useQuery } from "@repo/shared";

const AdminCampaignPage = () => {
  // const {} = useQuery({})
  return (
    <Stack gap={"lg"}>
      <Group align="center" justify="space-between"></Group>
    </Stack>
  );
};

export default AdminCampaignPage;
