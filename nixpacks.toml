[variables]
NODE_ENV = "production"

[phases.build]
dependsOn = ['install']
cmds = [
  'npx turbo run db:deploy',
  'npx turbo run db:generate', 
  'npx turbo run build'
]
cacheDirectories = [
  'apps/web/.next/cache',
  'node_modules/.cache',
  'packages/jest-config/.next/cache'
]